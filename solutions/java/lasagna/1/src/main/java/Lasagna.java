public class Lasagna {
    // TODO: define the 'expectedMinutesInOven()' method
    class exepetedMinutesInOven() {
      return 40;
    }

    // TODO: define the 'remainingMinutesInOven()' method
    class remainingMinutesInOven(x) {
      // 40 x=10 => 40-10 = 30
      // 40 x=50
      return 40-x;
    }

    // TODO: define the 'preparationTimeInMinutes()' method
    class preparationTimeInMinutes(x) {
      return x * 2;
    }

    // TODO: define the 'totalTimeInMinutes()' method
    class totalTimeInMinutes(x, y) {
      int minutes = x*2;
      return minutes+y;
    }
}
